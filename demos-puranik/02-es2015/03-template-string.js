const message = 'Hello', name = 'world';
// const greeting = message + ' ' + name + '!'
const greeting = `${message} ${name}!`; // we are using backticks to enclose the string - `` and not ''

console.log( greeting );